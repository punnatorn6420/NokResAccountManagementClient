{
  "type": "object",
  "required": [ "name", "environment" ],
  "properties": {
    "name": {
      "type": "string",
      "example": "Mobile App Key",
      "description": "ชื่อเรียกของ Credential ชุดนี้"
    },
    "environment": {
      "type": "string",
      "enum": [ "PROD", "UAT" ],
      "example": "UAT"
    },
    "active": {
      "type": "boolean",
      "default": true
    }
  }
}